schema_version = 1
id = "pr"
title = "PR Verification Metadata"
area = "suite"
owner = "verification"
status = "planned"
last_reviewed = "2026-07-08"
purpose = "Protect verification metadata links before broader repo gates."
placeholder = true
placeholder_note = "This Phase 1 seed is metadata-only. It is not the final PR gate until VERIF-P5-000A defines suite composition."
duration_class = "fast"
environment = "trust-builder"
commands = [
  "python3 scripts/validate_verification_metadata.py"
]
evidence_destination = "target/gate-artifacts/verification-metadata/"
includes = ["veryquick"]
excludes = ["hardware_lab"]
approved_proof_producers = []
