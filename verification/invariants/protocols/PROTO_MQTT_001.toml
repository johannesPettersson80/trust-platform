schema_version = 1
id = "PROTO_MQTT_001"
title = "MQTT discovery confidence and session lifetime are honest"
area = "protocols"
risk = "false_status"
status = "implemented"
owner = "trust-runtime"
last_reviewed = "2026-07-10"
claim = "MQTT discovery must distinguish a protocol handshake from port reachability and must not leave discovery sessions active without a contract."
proof_level = "G1"
contract_kind = "protocol_trace"
source_refs = ["crates/trust-runtime/src"]
tests = ["TEST_PROTOCOL_DISCOVERY_CONFIDENCE_TRACE_001"]
gates = []
evidence_refs = [
  "EVID_TEST_PROTOCOL_DISCOVERY_CONFIDENCE_TRACE_001_LOCK_BASELINE",
  "EVID_TEST_PROTOCOL_DISCOVERY_CONFIDENCE_TRACE_001_LOCK_COMPARE",
]
spec_gap_refs = []
missing = ["broad_remote_gate", "physical_broker_interop_evidence"]

[spec]
status = "specified"
source_refs = ["SPEC_RUNTIME_ENGINE_001"]

[oracle]
kind = "trust_contract"
ref = "SPEC_RUNTIME_ENGINE_001"

[[coverage.cells]]
dimension = "ordering_or_lifecycle"
state = "covered"
rationale = "The current-contract lock comparison covers accepted, rejected, auth-rejected, and TCP-only outcomes plus clean-session disconnect; a broad gate and physical-broker interoperability evidence remain open."
