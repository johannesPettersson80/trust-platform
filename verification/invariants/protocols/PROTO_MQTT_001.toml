schema_version = 1
id = "PROTO_MQTT_001"
title = "MQTT discovery confidence and session lifetime are honest"
area = "protocols"
risk = "false_status"
status = "gap_open"
owner = "trust-runtime"
last_reviewed = "2026-07-10"
claim = "MQTT discovery must distinguish a protocol handshake from port reachability and must not leave discovery sessions active without a contract."
proof_level = "S0"
contract_kind = "protocol_trace"
source_refs = ["crates/trust-runtime/src"]
tests = ["TEST_PROTOCOL_DISCOVERY_CONFIDENCE_TRACE_001"]
gates = []
evidence_refs = []
spec_gap_refs = []
missing = ["producer_authentic_current_lock_proof", "broad_remote_gate"]

[spec]
status = "specified"
source_refs = ["SPEC_RUNTIME_ENGINE_001"]

[oracle]
kind = "trust_contract"
ref = "SPEC_RUNTIME_ENGINE_001"

[[coverage.cells]]
dimension = "ordering_or_lifecycle"
state = "gap_open"
rationale = "The written discovery trace covers accepted, rejected, auth-rejected, and TCP-only outcomes plus clean-session disconnect; a current-contract lock comparison and broad remote gate remain open."
