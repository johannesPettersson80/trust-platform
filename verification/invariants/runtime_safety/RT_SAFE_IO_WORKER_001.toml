schema_version = 1
id = "RT_SAFE_IO_WORKER_001"
title = "I/O workers cannot hide scan-thread safety failures"
area = "runtime_safety"
risk = "safety_critical"
status = "implemented"
owner = "trust-runtime"
last_reviewed = "2026-07-14"
claim = "Protocol worker queues, reconnect loops, and snapshots must remain bounded and must expose stale or failed safe-state handoff truthfully."
proof_level = "G2"
contract_kind = "state_machine"
source_refs = ["crates/trust-runtime/src/io"]
tests = ["TEST_RUNTIME_SAFE_STATE_HANDOFF_001"]
gates = ["pr"]
evidence_refs = [
  "EVID_TEST_RUNTIME_SAFE_STATE_HANDOFF_001_RED",
  "EVID_TEST_RUNTIME_SAFE_STATE_HANDOFF_001_GREEN",
  "EVID_RUNTIME_SAFE_STATE_GAP_CLOSEOUT_20260714",
  "EVID_BROAD_REMOTE_PR_20260714_30F1B77C35BE",
]
spec_gap_refs = []
missing = ["hardware_lab_worker_disconnect"]

[spec]
status = "specified"
source_refs = ["SPEC_RUNTIME_ENGINE_001"]

[oracle]
kind = "trust_contract"
ref = "SPEC_RUNTIME_ENGINE_001"

[[behavior]]
partition = { equals = "WORKER_SAFE_STATE_UNCONFIRMED" }
case_family = "concurrency_or_cancellation"
oracle_ref = "SPEC_RUNTIME_ENGINE_001#safe-state-handoff-confirmation"
outcome = "not_safe_reported"
fault_surface = "SafeStateUnconfirmed"
delta = { target = "set_to_expected", siblings = "expected_delta", retain = "not_applicable", process_image = "expected_delta", diagnostics = "stable_error", status = "not_safe_reported" }
rationale = "A queued, reconnecting, timed-out, degraded, or faulted worker handoff is bounded but cannot be reported as confirmed physical safe state; later configured drivers are still attempted."

[[coverage.cells]]
dimension = "concurrency_or_cancellation"
state = "covered"
rationale = "Producer-authentic red and green proof covers bounded unconfirmed worker handoffs and continued delivery attempts; broad remote and hardware-disconnect evidence remain explicit invariant debt."
