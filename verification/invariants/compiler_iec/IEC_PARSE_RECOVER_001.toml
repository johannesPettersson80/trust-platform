schema_version = 1
id = "IEC_PARSE_RECOVER_001"
title = "Parser recovery advances and reports malformed structure"
area = "compiler_iec"
risk = "silent_corruption"
status = "implemented"
owner = "trust-syntax"
last_reviewed = "2026-07-14"
claim = "Parser recovery must not silently accept malformed delimiter or nesting structure."
proof_level = "G1"
contract_kind = "decision_table"
source_refs = ["crates/trust-syntax/src"]
tests = [
  "TEST_IEC_PARSER_RECOVERY_TRACE_001",
  "TEST_IEC_PARSER_REQUIRED_DELIMITERS_001",
  "TEST_IEC_PARSER_NESTED_TERMINATOR_RECOVERY_001",
  "TEST_IEC_PARSER_MISSING_POU_TERMINATOR_001",
  "TEST_IEC_PARSER_UNKNOWN_TOKEN_PROGRESS_001",
  "TEST_IEC_PARSER_DEEP_UNARY_BOUND_001",
  "TEST_IEC_PARSER_DEEP_PAREN_BOUND_001",
  "TEST_IEC_PARSER_UNCLOSED_CALL_001",
]
gates = ["veryquick", "pr"]
evidence_refs = [
  "EVID_TEST_IEC_PARSER_RECOVERY_TRACE_001_LOCK_BASELINE",
  "EVID_TEST_IEC_PARSER_RECOVERY_TRACE_001_LOCK_COMPARE",
]
spec_gap_refs = []
missing = ["broad_remote_gate"]

[input]
name = "source"
type = "Structured Text source"
declared = "IEC selection, iteration, POU, or expression syntax"

[spec]
status = "specified"
source_refs = ["SPEC_IEC_DECISIONS_001"]

[oracle]
kind = "trust_contract"
ref = "SPEC_IEC_DECISIONS_001"

[[behavior]]
partition = { equals = "CASE_MISSING_OF" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "CASE without OF is rejected with the reviewed required-token diagnostic."

[[behavior]]
partition = { equals = "CASE_LABEL_MISSING_COLON" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "A CASE label without its colon is rejected visibly."

[[behavior]]
partition = { equals = "ELSIF_MISSING_THEN" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "ELSIF without THEN is rejected visibly."

[[behavior]]
partition = { equals = "FOR_MISSING_CONTROL_VARIABLE" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "FOR without a control variable is rejected visibly."

[[behavior]]
partition = { equals = "FOR_MISSING_ASSIGNMENT" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "FOR without its assignment token is rejected visibly."

[[behavior]]
partition = { equals = "FOR_MISSING_TO" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "FOR without TO is rejected visibly."

[[behavior]]
partition = { equals = "FOR_MISSING_DO" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "FOR without DO is rejected visibly."

[[behavior]]
partition = { equals = "WHILE_MISSING_DO" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "WHILE without DO is rejected visibly."

[[behavior]]
partition = { equals = "REPEAT_MISSING_UNTIL" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "REPEAT without UNTIL is rejected visibly."

[[behavior]]
partition = { equals = "MISSING_INNER_TERMINATOR_AT_OUTER_BOUNDARY" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "The inner construct diagnoses its missing terminator without consuming the outer terminator or following statement."

[[behavior]]
partition = { equals = "MISSING_POU_TERMINATOR_AT_EOF" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "End-of-file before the owning POU terminator produces a visible parse failure."

[[behavior]]
partition = { equals = "UNKNOWN_TOKEN" }
case_family = "extra_or_unknown"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "Unknown tokens diagnose and recovery advances or stops at a synchronization boundary."

[[behavior]]
partition = { equals = "UNCLOSED_CALL_DELIMITER" }
case_family = "wrong_type_or_shape"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "An unclosed call delimiter fails visibly at a bounded expression or statement boundary."

[[behavior]]
partition = { equals = "DEEP_UNARY_NESTING" }
case_family = "resource_limit"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "Deep unary expressions stop at the parser nesting limit with a diagnostic instead of recursing without bound."

[[behavior]]
partition = { equals = "DEEP_PAREN_NESTING" }
case_family = "resource_limit"
oracle_ref = "SPEC_IEC_DECISIONS_001#malformed-structured-text-parser-recovery-policy"
outcome = "reject"
no_partial_apply = true
delta = { target = "unchanged", siblings = "unchanged", retain = "unchanged", process_image = "unchanged", diagnostics = "expected_diagnostic", status = "unchanged" }
rationale = "Deep parenthesized expressions stop at the parser nesting limit with a diagnostic instead of recursing without bound."

[[coverage.cells]]
dimension = "wrong_type_or_shape"
state = "covered"
rationale = "The committed current-contract proof pair covers malformed delimiters and terminators; a causal broad gate remains open."

[[coverage.cells]]
dimension = "extra_or_unknown"
state = "covered"
rationale = "The committed current-contract proof pair covers visible unknown-token rejection and recovery progress; a causal broad gate remains open."

[[coverage.cells]]
dimension = "resource_limit"
state = "covered"
rationale = "The committed current-contract proof pair covers bounded rejection of deep unary and parenthesized expressions; a causal broad gate remains open."
