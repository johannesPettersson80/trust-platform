# Runtime Reliability Bundle Fixture

This project folder is the tracked fixture used by:

- `scripts/runtime_load_test.sh`
- `scripts/runtime_soak_test.sh`
- `.github/workflows/nightly-reliability.yml`

The load/soak scripts build `program.stbc` from `sources/` before execution.
