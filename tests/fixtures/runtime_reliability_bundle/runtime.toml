[bundle]
version = 1

[resource]
name = "ReliabilityResource"
cycle_interval_ms = 100

[runtime.control]
endpoint = "unix:///tmp/trust-runtime-reliability.sock"
mode = "production"
debug_enabled = false

[runtime.web]
enabled = false
listen = "127.0.0.1:18080"
auth = "local"

[runtime.discovery]
enabled = false
service_name = "truST-reliability"
advertise = false
interfaces = []

[runtime.mesh]
enabled = false
listen = "127.0.0.1:15200"
auth_token = ""
publish = []
subscribe = {}

[runtime.log]
level = "info"

[runtime.retain]
mode = "none"
save_interval_ms = 1000

[runtime.watchdog]
enabled = false
timeout_ms = 5000
action = "halt"

[runtime.fault]
policy = "halt"
