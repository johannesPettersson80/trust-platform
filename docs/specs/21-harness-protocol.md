# Harness Protocol

This specification owns the deterministic JSON-line protocol used by
`trust-harness` and harness-compatible runtime automation.

## 1. Scope

The harness protocol defines deterministic execution requests, result framing,
and machine-readable event output for repeatable tests and simulations.

## 2. Canonical Contract

--8<-- "docs/guides/TRUST_HARNESS_PROTOCOL.md"
