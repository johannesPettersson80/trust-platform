# Snapshot Provenance: initial-review-input/

Captured: 2026-07-08, immediately after the review-edit fold pass (the fold
landed 08:18-08:20 local time, before this snapshot could be taken).

- The external review (Fable, verdict `clear-with-edits`) was performed against
  the pre-fold document set as it existed 2026-07-08 ~02:00.
- Between review delivery and this snapshot, required edits were already folded
  into six of the seven documents, so `initial-review-input/` holds the
  post-fold state at fold time, before the reviewer's residual fixes.
- `initial-review-input/fable-review-brief.md` is byte-identical to the
  reviewed version (unchanged since 2026-07-08 01:59).
- Exact pre-fold text for each finding is quoted in `review-verdict.md` where
  the finding cites it.
- `initial-review-input/entrypoint-checklist.md` is a copy of
  `docs/internal/testing/checklists/plc-verification-program-checklist.md`.
- `initial-review-input/README.md` is the snapshotted program README, not a
  description of the snapshot; this file is the snapshot description.
