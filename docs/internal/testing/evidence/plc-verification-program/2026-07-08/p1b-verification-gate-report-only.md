# P1B Verification Gate Report-Only CI Path

Date: 2026-07-09

Implemented row:

- `VERIF-P1B-010`: cheap report-only verification-gate CI path.

What changed:

- Added `scripts/verification/report_gate.py`.
- Added `scripts/verification_report_gate.py` as the thin CLI entrypoint.
- Added tests first in `scripts/verification/report_gate_tests.py`.
- Added `.github/workflows/verification-gate.yml`.
- The workflow runs on pull requests and `workflow_dispatch`, writes Markdown
  and JSON reports under `gate-artifacts/verification/`, adds the Markdown
  report to the GitHub step summary, and uploads the report artifact.

Report contents:

- Runs `scripts/verification_metadata_gate.sh`, which validates metadata and
  runs derived-path `gen_cases.py --check` for every cataloged case table.
- Re-derives `plan_tests.py --intent bugfix --changed <PR diff> --format json`
  from the pull-request diff.
- Reports changed test-like files that do not have a catalog row.

Burn-in behavior:

- The CLI default is report-only and returns exit `0` even when the metadata
  gate, planner, or catalog-ratchet report findings.
- A `--strict` mode exists for later enforcement, but the workflow does not use
  it.
- The workflow does not enforce outside the bytecode/VM pilot during burn-in.
- `VERIF-STOP-012` remains closed; no skills or agent mandates were updated.

Stop boundary:

- No runtime, VM, compiler, LSP, VS Code extension, or product behavior changed.
- No spec gap was closed.
- No proof evidence was generated by `prove.py`.
- No release metadata changed.

Focused validation run:

- `python3 -m unittest scripts.verification.report_gate_tests`
  - Result: 3 tests passed.
- `python3 scripts/verification_report_gate.py --changed crates/trust-runtime/src/bytecode/validate/pou_and_instr.rs crates/trust-runtime/tests/new_verification_report_smoke.rs --intent bugfix --out-dir /tmp/trust-verification-gate-test`
  - Result: exit `0`.
  - Reported `verification_metadata_gate` exit `0`.
  - Reported planner exit `4` because the synthetic new test path is unmapped
    by the bytecode/VM pilot matrix.
  - Reported the synthetic test path as uncataloged.
- `bash -lc 'python3 scripts/plan_tests.py --intent bugfix --changed crates/trust-runtime/src/bytecode/validate/pou_and_instr.rs --format json; status=$?; test "$status" -eq 3'`
  - Result: exit `3`, mapped to `bytecode_vm`, with the expected open
    bytecode/VM spec gaps.
- `python3 -m py_compile scripts/verification_report_gate.py scripts/verification/report_gate.py scripts/verification/report_gate_tests.py`
  - Result: passed.
- `python3 scripts/validate_verification_metadata.py`
  - Expected current result after this evidence row is indexed:
    `verification metadata validated: 95 records`.
- `scripts/verification_metadata_gate.sh`
  - Expected current result after this evidence row is indexed:
    `verification metadata validated: 95 records`.
- `git diff --check`
  - Result: passed.
