# P1B Bytecode Transform Generator

Date: 2026-07-09

Implemented row:

- `VERIF-P1B-009`: one deterministic bytecode transform generator for
  container truncation by section boundary, unknown opcode sweep, jump-target
  sweep, and stack-underflow cases from committed seed artifacts.

What changed:

- Added `scripts/verification/bytecode_transforms.py` as a separate generator
  module. It does not grow `prove.py` or the metadata validator.
- Added `scripts/verification/bytecode_transforms_tests.py` first. The initial
  run failed on the missing module before implementation.
- Added committed seed artifact
  `verification/seeds/bytecode_vm/minimal-stbc-seed.toml`.
- Added `transform_seed` metadata to
  `verification/invariants/bytecode_vm/VM_SEAM_VALID_001.toml`.
- Generated and committed
  `verification/cases/bytecode_vm/VM_SEAM_VALID_001.toml`.
- Refreshed the three existing case tables because the generator source digest
  now also includes `bytecode_transforms.py`.
- Added `TEST_CASE_TABLE_VM_SEAM_VALID_001` to
  `verification/test-catalog.toml`.
- Updated `verification/README.md` to document committed transform seed
  artifacts.

Generated transform cases:

- 2 `missing_required` container truncation cases.
- 2 `extra_or_unknown` unknown opcode cases.
- 2 `wrong_type_or_shape` jump-target cases.
- 1 `wrong_type_or_shape` stack-underflow case.

Current proof status:

- All generated transform cases are `state = "blocked"` with
  `spec_gap_ref = "SPEC_GAP_BYTECODE_VALIDATOR_001"`.
- No expected outcomes, deltas, or error codes were generated.
- No bytecode validator behavior was changed or claimed.

Stop boundary:

- No runtime, VM, compiler, LSP, VS Code, or product behavior changed.
- No spec gap was closed.
- No proof evidence was generated by `prove.py`.
- No CI enforcement, release metadata, or skills changed.
- Other generated families remain out of v1.

Focused validation run:

- `python3 -m unittest scripts.verification.bytecode_transforms_tests`
  - Result: 6 tests passed.
- `python3 -m unittest scripts.verification.prover_tests scripts.verification.metadata_validator.evidence_proof_tests`
  - Result: 63 tests passed.
- `python3 scripts/gen_cases.py --invariant VM_SEAM_SUBRANGE_001 --check`
  - Result: passed.
- `python3 scripts/gen_cases.py --invariant VM_SEAM_DECLARED_TYPE_001 --check`
  - Result: passed.
- `python3 scripts/gen_cases.py --invariant VM_SEAM_STRING_BOUND_001 --check`
  - Result: passed.
- `python3 scripts/gen_cases.py --invariant VM_SEAM_VALID_001 --check`
  - Result: passed.
- `python3 -m py_compile scripts/gen_cases.py scripts/verification/case_generator.py scripts/verification/bytecode_transforms.py scripts/verification/bytecode_transforms_tests.py scripts/verification/case_digests.py`
  - Result: passed.
- `python3 scripts/validate_verification_metadata.py`
  - Expected current result after this evidence row is indexed:
    `verification metadata validated: 94 records`.
- `scripts/verification_metadata_gate.sh`
  - Expected current result after this evidence row is indexed:
    `verification metadata validated: 94 records`.
- `git diff --check`
  - Result: passed.
