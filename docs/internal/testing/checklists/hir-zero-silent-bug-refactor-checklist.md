# HIR Zero-Silent-Bug Refactor Checklist

Status: Complete; final full merge gate passed
Owner: HIR/runtime compiler team
Scope: close all known HIR architecture paths that can allow silent semantic bugs, not only namespace bugs.

Investigation: `docs/internal/architecture/hir-zero-silent-bug-refactor-investigation-2026-04-29.md`.

## Stop Rules

- [x] `HIRZSB-STOP-01` Do not fix a HIR, parser-to-HIR, type-check, diagnostic, import, initializer, OOP, runtime-lowering, or OSCAT aggregate bug without first adding or identifying a focused failing test. Evidence: the final OSCAT blocker was first locked with `test_function_return_variable_can_be_read_inside_function` and `test_method_return_variable_can_be_read_inside_method`, both failing before the `NameRef` return-variable fix.
- [x] `HIRZSB-STOP-02` Do not add namespace-sensitive, kind-sensitive, project-sensitive, or diagnostic-bearing broad lookups outside the semantic kernel allowlist. Evidence: `python3 scripts/hir_zero_silent_bug_doctor.py` reports no broad-lookup findings.
- [x] `HIRZSB-STOP-03` Do not collapse `Unknown`, `Ambiguous`, `WrongKind`, unsupported syntax, internal invariant failure, and intentional cascade suppression into the same silent `None` inside HIR semantic internals. Evidence: `SemanticOutcome` migration plus `cargo mutants` shards had 0 missed mutants across the focused semantic-kernel campaign.
- [x] `HIRZSB-STOP-04` Do not add duplicated resolver/type-resolution/import-resolution rules in diagnostics, type checking, OOP checks, or runtime lowering. Evidence: `HIRZSB-P6-002` doctor rule reports no duplicated-resolver findings.
- [x] `HIRZSB-STOP-05` Do not expand runtime syntax-walk declaration discovery without a HIR/runtime declaration parity test. Evidence: runtime POU registration is catalog driven and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test initializer_architecture runtime_pou_registration_is_hir_catalog_driven -- --nocapture` passed earlier in this board.
- [x] `HIRZSB-STOP-06` Do not re-enable a one-compile OSCAT aggregate replacement until the HIR zero-silent-bug parity gates pass. Evidence: the default gate remains structural/fast; the one-compile aggregate trigger is a focused regression fixture and now passes with the raised CPU-bound child timeout.
- [x] `HIRZSB-STOP-07` Do not add new `TypeId::UNKNOWN` or `SymbolId::UNKNOWN` sentinel returns in diagnostic-bearing semantic code outside an explicit allowlist with a test and rationale. Evidence: `docs/internal/architecture/hir-zero-silent-bug-allowlist-artifacts-2026-04-30.md` records no active typed-sentinel allowlist beyond explicit compatibility boundaries, and the doctor reports no typed-sentinel findings.
- [x] `HIRZSB-STOP-08` Do not discard collision-bearing semantic return values with `let _ = ...`; handle the collision, emit a diagnostic, or document an allowlisted invariant with a test. Evidence: the allowlist artifact records no active silent-discard allowlist, and the doctor reports no silent-discard findings.

## Phase 0 - Evidence And Scope

- [x] `HIRZSB-P0-001` Read the full software-map audit and record that the existing architecture doctor is useful but not a full HIR silent-error guard.
- [x] `HIRZSB-P0-002` Read the completed HIR mutation board and record that it covered `symbol_import`, `type_check::const_eval`, and `collector/variables`, not full HIR semantic architecture.
- [x] `HIRZSB-P0-003` Inspect live HIR resolver, collector, diagnostic, OOP interface, and runtime lowering paths and record duplicated ownership in the investigation.
- [x] `HIRZSB-P0-004` Add this full HIR zero-silent-bug execution checklist and wire it into the umbrella architecture program.

## Phase 1 - Red Behavior-Lock Tests For The Known Trigger

- [x] `HIRZSB-P1-001` Add a failing test where `FUNCTION_BLOCK` or `CLASS` in namespace `A` implements a sibling namespace interface without resolving an unrelated global interface of the same bare name. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_interfaces namespaced_ -- --nocapture` covers `namespaced_function_block_implements_sibling_interface_not_global_bare_name`.
- [x] `HIRZSB-P1-002` Add a failing test where an interface extends a sibling interface in the same namespace and does not resolve through a global bare-name fallback. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_interfaces namespaced_ -- --nocapture` failed before the fix with `InvalidOperation`, then passed after inherited-interface member collection used scope-aware `resolve_extends_symbol`.
- [x] `HIRZSB-P1-003` Add a failing test where duplicate bare type names in different namespaces force scoped/qualified resolution instead of broad fallback. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces -- --nocapture` covers `namespaced_type_reference_prefers_scoped_type_over_global_bare_name`.
- [x] `HIRZSB-P1-004` Add a failing test where constants in same-named POUs under different namespaces do not collide. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces -- --nocapture` covers `same_named_pou_constants_in_different_namespaces_do_not_collide`.
- [x] `HIRZSB-P1-005` Add a failing test where `USING` ambiguity produces the exact intended diagnostic code/location. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces using_ambiguous_name_reports_cannot_resolve_without_choosing_candidate -- --nocapture` failed before the fix because ambiguity emitted `CannotResolve` plus wrong-reason `UndefinedFunction`, then passed after call inference consumed the explicit ambiguous outcome.
- [x] `HIRZSB-P1-006` Add a failing test proving diagnostics and type checking resolve the same namespace-qualified type reference. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces namespace_qualified_type_reference_matches_type_check_resolution -- --nocapture` passes with the same namespace-qualified type reference accepted by diagnostics and type checking.
- [x] `HIRZSB-P1-007` Add a failing runtime/HIR parity test for namespaced `PROGRAM`, `FUNCTION`, `FUNCTION_BLOCK`, `CLASS`, and `INTERFACE` declaration discovery. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test e2e_multifile namespaced_pous_resolve_sibling_interfaces -- --nocapture` first exposed that the runtime bytecode path did not resolve namespaced sibling function calls and owner-local methods correctly, then passed after codegen carried POU `USING` namespaces, kept method names owner-local, and exposed bare return-variable aliases for namespaced functions.
- [x] `HIRZSB-P1-008` Distill the original OSCAT aggregate failure into a permanent focused HIR/runtime regression fixture before retrying the aggregate sweep. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test oscat_oop_examples oscat_airport_baggage_namespace_aggregate_trigger_passes -- --nocapture` passes after permanently aggregating the original `airport_baggage_command_observer/oop` trigger into one namespace-wrapped temp project and running its 3 ST tests through the runtime CLI.

## Phase 2 - Red Behavior-Lock Tests For Broader Silent-Error Classes

- [x] `HIRZSB-P2-001` Add wrong-kind resolution tests: value used as type, type used as value, namespace used as callable, and callable used as variable must produce exact diagnostics. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking wrong_kind_resolution -- --nocapture` failed before the fix because value-as-type degraded to `UndefinedType` while type-as-value and callable-as-variable were silent, then passed after pending type resolution and expression name inference emitted exact wrong-kind diagnostics without wrong-reason cascades.
- [x] `HIRZSB-P2-002` Add ambiguous import/project resolution tests that prove HIR cannot silently choose one candidate. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces cross_file_using_ambiguous_name_reports_cannot_resolve_without_choosing_import -- --nocapture` covers two imported namespaces exporting the same callable and proves HIR emits `CannotResolve` without wrong-reason `UndefinedFunction`; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir diagnostics_salsa_reports_duplicate_cross_file_type_declarations -- --nocapture` keeps duplicate project type declarations diagnostic-bearing instead of first-writer selection.
- [x] `HIRZSB-P2-003` Add tests proving every intentional diagnostic cascade suppression has an earlier primary diagnostic. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking suppression_has_primary -- --nocapture` covers assignment compatibility, binary expression inference, and `is_assignable` call-argument suppression; the binary expression test failed before the fix with wrong-reason `TypeMismatch`, then passed after numeric/bit/unary inference returned `UNKNOWN` without cascade diagnostics when an operand already had a primary error.
- [x] `HIRZSB-P2-004` Add tests for symbol-owner scope lookup failure so missing owner/scope cannot be treated as successful validation. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::context::tests -- --nocapture`, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir missing_pou_owner -- --nocapture`, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::oop::tests::extends_resolution_does_not_fallback_to_global_when_owner_scope_is_missing -- --nocapture` prove POU context and OOP `EXTENDS` resolution classify missing owner/scope instead of silently falling back to global scope.
- [x] `HIRZSB-P2-005` Add tests for TypeId and initializer ID translation so cross-project imports cannot silently reuse the wrong identity. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking cross_file_import_preserves_all_compound_type_shapes_under_type_id_collisions -- --nocapture` covers TypeId remapping under target-side collisions; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking cross_file_import_translates_struct_field_default_initializer -- --nocapture` and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking cross_file_import_translates_union_variant_default_initializer -- --nocapture` cover imported initializer IDs resolving in the target catalog.
- [x] `HIRZSB-P2-006` Add tests for unsupported syntax shapes in HIR validation so each path is classified as diagnostic, cascade suppression, or explicit non-goal. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_var_config_undefined_target_error -- --nocapture` failed before the fix because an unresolved `VAR_CONFIG` target produced no diagnostics, then passed after HIR validation emitted `UndefinedVariable`; `collect_var_config_block` now classifies malformed/out-of-scope `VAR_CONFIG` entries instead of remaining an empty no-op.
- [x] `HIRZSB-P2-007` Add tests for callable/value/type role distinctions across project imports and namespaces. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking wrong_kind_resolution -- --nocapture` failed before the fix because imported namespaced type `Library.Widget` used as a value emitted no diagnostic, then passed after namespace-qualified field-expression resolution reused the same non-value role check as bare name resolution; nearby `control_flow_and_calls` and `types_and_references` focused suites also passed.
- [x] `HIRZSB-P2-008` Add a cross-project symbol-name import collision red test: two imported files export the same global symbol name and HIR emits a duplicate-import diagnostic instead of silently keeping the first binding. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_cross_file_global_import_collision_reports_duplicate -- --nocapture` failed before the fix with no diagnostics, then passed after `SymbolImporter` recorded skipped import-name collisions and project diagnostics emitted `DuplicateDeclaration`; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 58 tests.
- [x] `HIRZSB-P2-009` Add a cyclic cross-project type import red test: the cycle emits a specific diagnostic or proves an earlier primary diagnostic before any `TypeId::UNKNOWN` cascade suppression. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_cyclic_cross_file_type_import_reports_primary_diagnostic -- --nocapture` failed before the fix with no diagnostics, then passed after project type import cycle detection emitted `CyclicDependency` and suppressed wrong-reason `UndefinedType` cascades for the active import stack; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking types_and_references -- --nocapture` passed 21 tests.
- [x] `HIRZSB-P2-010` Add tests for every `eval_const_int_expr` caller that currently depends on `.ok()`/`Option`, proving each `ConstEvalError` variant is either diagnosed or intentionally cascade-suppressed with a primary diagnostic. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking const_eval_error -- --nocapture` covers array-index, subrange-assignment, and `SIZEOF` type-operand const-eval failures; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking type_resolution_const_eval_errors_report_primary_diagnostic -- --nocapture` failed when `STRING[4 / 0]` collapsed to the generic `TypeMismatch`, then passed after collector type lowering used `try_eval_optional_int_expr_in_scope` and reported the primary `DivideByZero` diagnostic. The type-check bridge was replaced with `eval_const_int_expr_or_report` and `require_const_int_expr`, and `rg -n "eval_const_int_expr\\(" crates/trust-hir/src/type_check` now finds only the new `try_eval_const_int_expr` call sites, not an `.ok()`/`Option` collapse. Follow-up suites `assignments_and_var_access`, `sizeof_semantics`, and `control_flow_and_calls` passed.
- [x] `HIRZSB-P2-011` Add a `VAR_ACCESS` undefined-target red test so missing access targets produce `UndefinedVariable` instead of silently returning. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_var_access_undefined_target_error -- --nocapture` failed before the fix with no diagnostics, then passed after `check_access_decl` emitted `UndefinedVariable`; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 54 tests.
- [x] `HIRZSB-P2-012` Add a non-callable `FieldExpr` callee red test so a field in call position emits a not-callable diagnostic instead of returning `TypeId::UNKNOWN`. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_struct_field_in_callee_position_is_not_callable -- --nocapture` failed before the fix with no diagnostics, then passed after the `FieldExpr` callee branch emitted `UndefinedFunction` for resolved non-callable fields while preserving primary diagnostics for unresolved fields.
- [x] `HIRZSB-P2-013` Add a `VAR_CONFIG` declaration-block red test so collector-level `VAR_CONFIG` either lowers correctly or emits a documented unsupported diagnostic. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking var_config -- --nocapture` passed 8 `VAR_CONFIG` tests after the unresolved-target red test was added and the collector classified malformed/out-of-scope config entries explicitly.
- [x] `HIRZSB-P2-014` Add validation/diagnostics global/external link parity tests so duplicate `VAR_GLOBAL`, `VAR_EXTERNAL`, constant, initializer, and type-mismatch behavior cannot diverge between the collector path and project diagnostics path. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections duplicate_file_scope_global_names_are_rejected_by_collector_path -- --nocapture` covers the collector duplicate-`VAR_GLOBAL` path; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections -- --nocapture` passed 6 tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 60 tests covering `VAR_EXTERNAL` missing-global, type mismatch, constant parity, initializer rejection, and cross-project duplicate global import diagnostics.
- [x] `HIRZSB-P2-015` Add a type-check expression-context red test proving `expression_context`, `action_context`, and `pou_context` failures cannot silently fall back to global scope; each path must emit a diagnostic or return an explicit `SuppressedCascade` outcome. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::type_check::tests::type_check_reports_missing_pou_owner_instead_of_global_fallback -- --nocapture` proves type-check emits a primary `CannotResolve` instead of silently type-checking at global scope; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::context::tests -- --nocapture` proves `pou_context`, `expression_context`, and `action_context` classify missing owner/scope outcomes explicitly.
- [x] `HIRZSB-P2-016` Add enum-value ambiguity red tests: two enums in different scopes/namespaces with the same value name must not let bare const-eval pick the first `FxHashMap` match. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_ambiguous_unqualified_enum_variant_in_constant_initializer_is_rejected -- --nocapture` failed before the fix with no diagnostics, then passed after enum lookup returned an explicit ambiguous outcome; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking enum_unqualified_in_expressions -- --nocapture` passed 4 tests.
- [x] `HIRZSB-P2-017` Add `Project::insert_with_id` collision and `Project::normalize_path` lexical/canonical collision red tests. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir project::tests::insert_with_id_rejects_file_id_collision -- --nocapture` failed before the fix because explicit `FileId(7)` collision silently reallocated, then passed after `insert_with_id` rejected the collision explicitly; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir project::tests::noncanonical_fallback_path_does_not_collide_with_canonical_path -- --nocapture` failed before the fix because a noncanonical fallback path collapsed onto an existing canonical source key, then `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir project::tests -- --nocapture` passed after fallback normalization preserved `..` components when canonicalization fails.
- [x] `HIRZSB-P2-018` Add a mixed function-block/class inheritance-cycle red test and require a shared extends-chain walker. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::oop::tests::function_block_cycle_detection_walks_mixed_class_links -- --nocapture` failed before the fix because the function-block walker stopped at a class link, then passed after class/interface/function-block cycle checks shared one extends-chain walker with kind-specific continuation rules.
- [x] `HIRZSB-P2-019` Add targeted cascade-suppression tests for `type_check::compatibility::is_assignable`, proving every `TypeId::UNKNOWN` true result has an earlier primary diagnostic and no wrong-reason cascaded diagnostic. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_call_argument_unknown_type_suppression_has_primary_diagnostic -- --nocapture` passes with `UndefinedVariable` as the primary diagnostic and no wrong-reason `InvalidArgumentType` cascade.

## Phase 3 - HIR Semantic Kernel

- [x] `HIRZSB-P3-001` Introduce typed `QualifiedName`, `ScopePath`, and source/project identity structures where HIR semantic identity matters. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir semantic::tests -- --nocapture` covers `QualifiedName` construction/display and source-backed semantic identity scaffolding in the internal `semantic` module.
- [x] `HIRZSB-P3-002` Introduce explicit resolver/validation outcomes for `Resolved`, `Unknown`, `Ambiguous`, `WrongKind`, `SuppressedCascade`, and internal invariant failures. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir semantic::tests -- --nocapture` covers the `SemanticOutcome<T>` shape and proves non-resolved classifications survive mapping instead of collapsing into `Option::None`.
- [x] `HIRZSB-P3-003` Implement one semantic kernel covering builtin, qualified, scoped, USING, project import, type-symbol, callable/value/type, and interface inheritance resolution. Focused evidence: semantic decisions now route through `SemanticOutcome<T>` and scoped resolver contracts for builtin/type names, qualified paths, scoped/USING lookup, project imports, callable/value/type role checks, OOP `EXTENDS`/`IMPLEMENTS`, and standard/type-check inference suppression; the remaining compatibility wrappers are named in `docs/internal/architecture/hir-semantic-kernel-allowlist.toml` with a max of 5 entries. The duplicated `resolve_program_type` implementations in configuration diagnostics and shared-global task diagnostics were unified into one `ProgramTypeResolution` helper carrying the concrete `SymbolId`. Validation: `python3 scripts/hir_zero_silent_bug_doctor.py` reports `no findings`, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking -- --nocapture` passed 285 tests, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections -- --nocapture` passed 6 tests, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test declaration_catalog -- --nocapture` passed 2 tests, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking basics_and_warnings -- --nocapture` passed 33 tests after the shared program-type resolver extraction.
- [x] `HIRZSB-P3-004` Migrate diagnostics context helpers to the semantic kernel. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::context::tests -- --nocapture` passed after diagnostics context type-resolution helpers gained `SemanticOutcome` wrappers for resolved, unknown, wrong-kind, and invariant cases; the legacy `Option` helpers are now compatibility wrappers and the path no longer falls back through broad `lookup_type`.
- [x] `HIRZSB-P3-005` Migrate type-check resolver helpers and every type-check inference function that returns `TypeId::UNKNOWN` to semantic-kernel outcome types, including expression, call, literal, common numeric/bit-string, unary bit-string, and `type_check/standard/*` inference paths. Focused evidence: `type_check/calls/exprs.rs` now uses `SemanticOutcome<TypeId>` for index, field, deref, address, and partial-bit access; `type_check/calls/special.rs` now uses `SemanticOutcome<TypeId>` for `REF`, `NEW`, and `__DELETE`; `type_check/expr.rs`, `type_check/calls.rs`, and `type_check/standard/*` now route legacy `TypeId` API boundaries through classified unknown/suppressed/diagnostic conversions instead of direct sentinel returns; remaining old storage/query boundaries use the named `LEGACY_UNKNOWN_TYPE_ID` compatibility value. Validation: `python3 scripts/hir_zero_silent_bug_doctor.py | awk '/HIRZSB-WARN-TYPED-SENTINEL/ {print}'` returns no findings; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking -- --nocapture` passed 285 tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections -- --nocapture` passed 6 tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test declaration_catalog -- --nocapture` passed 2 tests.
- [x] `HIRZSB-P3-006` Migrate OOP `IMPLEMENTS` and `INTERFACE EXTENDS` resolution to the semantic kernel. Focused evidence: `check_interface_conformance` now resolves `IMPLEMENTS` targets through `resolve_type_symbol_by_name_in_scope_outcome`, and `resolve_extends_symbol_outcome` carries `INTERFACE EXTENDS` resolution as `SemanticOutcome<SymbolId>` instead of silently collapsing owner-scope, unknown, ambiguous, and wrong-kind cases into `Option::None`; the old unused `resolve_type_symbol_by_name_in_scope` wrapper was removed. Validation: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_interfaces -- --nocapture` passed 13 interface/extends/conformance tests, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::context::tests -- --nocapture` passed 5 semantic-context outcome tests.
- [x] `HIRZSB-P3-007` Migrate collector/import/initializer identity decisions that currently duplicate semantic rules. Focused evidence: project symbol import now routes scope insertion through `define_imported_symbol_in_scope` instead of `let _ = define_in_scope(...)` discards, recording import collisions when scope insertion would otherwise hide an existing symbol; `import_type_outcome` classifies imported type identity mapping as `SemanticOutcome<TypeId>` for resolved, cycle-suppressed, missing-source invariant, and unknown-source-type cases while keeping the legacy `TypeId` wrapper only at the call boundary. Initializer identity remains translated through the HIR initializer catalog. Validation: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_cross_file_global_import_collision_reports_duplicate -- --nocapture` passed, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking cross_file_import_translates_struct_field_default_initializer -- --nocapture` passed, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking cross_file_import_translates_union_variant_default_initializer -- --nocapture` passed, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_cyclic_cross_file_type_import_reports_primary_diagnostic -- --nocapture` passed, and `rg -n "let _ = .*define_in_scope|let _ = .*\\.insert\\(" crates/trust-hir/src/db/symbol_import.rs crates/trust-hir/src -g '*.rs'` found no silent discard matches.
- [x] `HIRZSB-P3-008` Leave any compatibility fallback as an explicit allowlisted call with a test and rationale. Focused evidence: `docs/internal/architecture/hir-semantic-kernel-allowlist.toml` now contains exactly 5 compatibility entries, each scoped to one named function with owner, rationale, review date `2026-04-29`, and focused test evidence: `SymbolTable::resolve_alias_type`, `TypeChecker::resolve_alias_type`, collector `resolve_type_path_at`, project-import `import_type`, and OOP diagnostics `resolve_extends_symbol`. The extra collector `resolve_type_path` wrapper was removed before allowlisting so the list stays at the numeric ceiling. Validation: `python3 scripts/hir_zero_silent_bug_doctor.py` exits 0 in warn-only mode with no allowlist-limit finding.
- [x] `HIRZSB-P3-009` Explicitly migrate `db/diagnostics/globals.rs::is_type_defined_in_scope_with_table`, `resolve_pending_types_with_table`, `db/queries/collector/types.rs::resolve_type_in_scope`, `resolve_type_path`, and `resolve_project_type_path`. Focused evidence: `is_type_defined_in_scope_with_table` was removed after `resolve_pending_types_with_table` moved through `resolve_type_symbol_by_name_in_scope_outcome`; collector type resolution now routes through `resolve_type_path_outcome`, `resolve_type_in_scope_outcome`, and `resolve_project_type_path_outcome`, preserving legacy `TypeId` wrappers only at the call boundary while classifying `Unknown`, `WrongKind`, `Ambiguous`, and cyclic project imports as `SuppressedCascade(CyclicDependency)`. Validation: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking types_and_references -- --nocapture` passed 21 tests, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking wrong_kind_resolution -- --nocapture` passed 7 exact wrong-kind tests, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 61 tests, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::context::tests -- --nocapture` passed 5 outcome/context tests.
- [x] `HIRZSB-P3-010` Explicitly migrate validation/diagnostics global-external link checks into one semantic path or prove parity with tests. Focused evidence: P2-014 parity coverage now locks both paths: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections -- --nocapture` covers collector duplicate/global section behavior and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` covers project diagnostics for `VAR_EXTERNAL` missing-global, type mismatch, constant parity, initializer rejection, and duplicate imported globals.
- [x] `HIRZSB-P3-011` Narrow `SymbolTable` broad lookup API after migration: `lookup_any`, broad `lookup_type`, and `resolve_by_name` become `pub(crate)` or are renamed with a `*_global_only` contract and an allowlist. Focused evidence: `lookup_any` is now `pub(crate)`, old `lookup_type` was removed after call-site migration, old `resolve_by_name` is `pub(crate)`, and public callers now use `lookup`, `lookup_registered_type_name`, or `resolve_global_or_qualified_name` contracts; integration tests that needed broad inspection now use explicit symbol iteration. Validation: `RUSTUP_TOOLCHAIN=1.95 cargo check -p trust-ide --all-targets`, `RUSTUP_TOOLCHAIN=1.95 cargo check -p trust-lsp --all-targets`, and `RUSTUP_TOOLCHAIN=1.95 cargo check -p trust-runtime --all-targets` all passed.
- [x] `HIRZSB-P3-012` Set the semantic-kernel compatibility allowlist target to at most 5 entries; each entry needs owner, rationale, review date, and a focused test. One allowlist entry means one named function or file:line range, not a subsystem category. Focused evidence: `docs/internal/architecture/hir-semantic-kernel-allowlist.toml` now exists with `max_entries = 5`, explicit entry granularity rules, and exactly 5 named compatibility entries (`SymbolTable::resolve_alias_type`, `TypeChecker::resolve_alias_type`, collector `resolve_type_path_at`, project-import `import_type`, and OOP diagnostics `resolve_extends_symbol`); `python3 scripts/hir_zero_silent_bug_doctor.py | rg "HIRZSB-ALLOWLIST"` returns no matches.
- [x] `HIRZSB-P3-013` Migrate alias resolution to the semantic kernel: `SymbolTable::resolve_alias_type`, `TypeChecker::resolve_alias_type`, and `resolve_subrange_base` must share one implementation and convert the silent depth-16 cap into an explicit cycle/depth-exceeded outcome. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_deep_alias_chain_resolves_to_base_type -- --nocapture` failed before the initial fix by resolving `Alias20` only to `Alias04`, then passed after `TypeChecker::resolve_alias_type` delegated to `SymbolTable::resolve_alias_type` and the table resolver used visited-set termination instead of a fixed 16-hop cap; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir symbols::table::tests::alias_resolution_outcome_reports_cycle_explicitly -- --nocapture` proves alias cycles now return an explicit `SemanticOutcome::InvariantViolation` while the legacy compatibility wrapper preserves existing `TypeId` behavior.
- [x] `HIRZSB-P3-014` Ship broad-lookup, sentinel-return, silent-discard, lossy `Result`-to-`Option`, `Type::Unknown` fallback, and duplicated-resolver doctor rules in warn-only mode at Phase 3 start, then flip them to fail in Phase 6. Focused evidence: `python3 scripts/hir_zero_silent_bug_doctor.py` exits 0 in warn-only mode and reports `HIR zero-silent-bug doctor: no findings`; broad lookup, typed sentinel, silent discard, lossy `try_*().ok()` collapse, `Type::Unknown` fallback, duplicated resolver, allowlist-limit, runtime-bypass, and public-raw-API rules are clean; `--fail` is present for the Phase 6 flip.
- [x] `HIRZSB-P3-015` Migrate `db/queries/collector/collect.rs::collect_program_config` and any sibling `collect_*_config` paths to the semantic kernel so broad `lookup_any` fallback and silent wrong-kind `TypeId::UNKNOWN` cannot remain. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_program_config_wrong_kind_type_reports_diagnostic -- --nocapture` failed before the fix with only wrong-reason `UndefinedType`, then passed after `collect_program_config` stopped using broad `lookup_any` and emitted `InvalidOperation` for non-`PROGRAM` symbols; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 61 tests, including cross-file program configuration cases that must remain unresolved until project merge.
- [x] `HIRZSB-P3-016` Tighten Phase 2 diagnostic tests so each red test asserts both the intended diagnostic and absence of wrong-reason cascaded diagnostics. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_program_config_wrong_kind_type_reports_diagnostic -- --nocapture` first exposed the remaining wrong-reason cascade (`[InvalidOperation, UndefinedType]`) for `PROGRAM` config wrong-kind resolution, then passed after `db/diagnostics/configuration.rs` classified wrong-kind program types separately from missing types; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking wrong_kind_resolution -- --nocapture` passed 7 exact wrong-kind/no-wrong-reason tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 61 tests including assignment/binary unknown suppression and program config no-wrong-reason assertions; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_cyclic_cross_file_type_import_reports_primary_diagnostic -- --nocapture` passed and locks the cyclic-import primary diagnostic without an `UndefinedType` cascade.

## Phase 4 - Full Semantic Identity

- [x] `HIRZSB-P4-001` Replace constant precollection keys based on optional bare POU name with full namespace/project-aware scope identity. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces same_named_pou_constants_in_different_namespaces_keep_distinct_values -- --nocapture` failed before the fix because `CellB.Main.values` used `[(0, 2)]` from `CellA.Main.Limit`, then passed after constant precollection and lookup used full identities such as `CellA.Main`/`CellB.Main`; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces -- --nocapture` passed 11 namespace tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking hir_mutation_hardening -- --nocapture` passed 22 const-eval hardening tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking types_and_references -- --nocapture` passed 25 type/reference tests.
- [x] `HIRZSB-P4-002` Audit symbol-owner keys, program-instance keys, TypeId translation, initializer ID translation, and project file identity for partial-key silent-error risk. Audit result: constant keys were partial and fixed in `HIRZSB-P4-001`; program-instance keys were partial because `P1` across multiple resources overwrote in `collect_program_instances`; symbol-owner scope keys are table-local `SymbolId -> ScopeId` records and are covered by missing-owner/scope tests; TypeId translation uses importer-local `(FileId, TypeId)` keys with source file identity; initializer import allocates fresh target-local `InitializerId`s; project file identity collision/coalescing was fixed under `HIRZSB-P2-017`. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking test_var_config_duplicate_program_instance_name_is_ambiguous -- --nocapture` failed before the program-instance fix with `[TypeMismatch, InvalidOperation]`, then passed after duplicate instance names became explicit ambiguity.
- [x] `HIRZSB-P4-003` Replace any partial key found in `HIRZSB-P4-002` or document a tested reason it is safe. Fixed partial keys: constant precollection now uses full namespace/POU scope identity, and program-instance lookup now uses `ProgramInstanceMap` entries that preserve `Resolved` versus `Ambiguous` instead of overwriting by bare instance name. Documented safe/tested identity surfaces: `scope_by_owner` remains table-local by `SymbolId`; `symbol_import::type_map` is importer-local and keyed by source `FileId` plus source `TypeId`; initializer IDs are copied into fresh target IDs; project source IDs reject explicit collisions and keep noncanonical fallback paths distinct. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces -- --nocapture` passed 11 tests, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 64 tests, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking basics_and_warnings -- --nocapture` passed 33 tests.
- [x] `HIRZSB-P4-004` Preserve global, namespace, POU, project-import, and cross-file semantics with exact diagnostics for undefined/ambiguous/cyclic cases. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces -- --nocapture` passed 11 namespace/USING/POU-constant tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 64 assignment/VAR_ACCESS/VAR_CONFIG tests including ambiguous program instances and cross-file import collision diagnostics; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking basics_and_warnings -- --nocapture` passed 33 warning/used-symbol tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections -- --nocapture` passed 6 global-section tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking types_and_references -- --nocapture` passed 25 type/reference tests after the constant-scope identity change.
- [x] `HIRZSB-P4-005` Enumerate and resolve or justify these known partial-identity surfaces: constant keys, diagnostics `GlobalKey`, `SymbolTable::extends`/`implements` raw strings, `symbol_import::type_map`, `Project::insert_with_id` collision behavior, and `Project::normalize_path` path coalescing. Resolution/justification: constant keys are full namespace/POU identities (`HIRZSB-P4-001`); diagnostics `GlobalKey` intentionally uses namespace path plus normalized name because same namespace/name globals are duplicates, with origin retained for related diagnostics; `SymbolTable::extends`/`implements` remain raw-string storage until the Phase 5 catalog replacement, but all member/compatibility consumers added in this phase now resolve raw names through `resolve_oop_reference_for_owner` using the owner scope instead of global fallback; `symbol_import::type_map` is importer-local and keyed by source `FileId` plus source `TypeId`, while target identity is the importer target table; `Project::insert_with_id` now rejects explicit `FileId` collisions; `Project::normalize_path` preserves noncanonical fallback identity. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces namespaced_extends_member_resolution_prefers_scoped_base_over_global -- --nocapture` failed before the scoped OOP-reference fix with `CannotResolve` for an inherited scoped member, then passed; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces -- --nocapture` passed 12 tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_interfaces -- --nocapture` passed 13 interface/extends tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking types_and_references -- --nocapture` passed 25 type/reference tests.
- [x] `HIRZSB-P4-006` Add a single-source POU-kind contract test or doctor rule so any new POU-like `SyntaxKind` is reflected in collector parent-stack, diagnostics context, type checking, and runtime lowering consumers; consolidate consumers onto one shared list or helper instead of duplicate ad hoc matches. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-syntax syntax::tests::test_pou_declaration_classifier_set -- --nocapture` failed before the shared classifier because `POU_DECLARATION_KINDS` and `SyntaxKind::is_pou_declaration` did not exist, then passed after `trust-syntax` became the single source for POU declaration kinds; HIR `is_pou_kind` now delegates to `SyntaxKind::is_pou_declaration`, and runtime expression lowering uses the same syntax helper instead of a duplicate local match. Validation: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir db::diagnostics::context::tests -- --nocapture` passed 5 HIR context tests and `RUSTUP_TOOLCHAIN=1.95 cargo check -p trust-runtime --all-targets` passed.
- [x] `HIRZSB-P4-007` Resolve `SymbolTable::add_symbol` versus `add_symbol_raw` `global_names` insertion-policy divergence; both paths must use the same first-wins/last-wins policy or emit the duplicate diagnostic before lookup state can diverge. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir symbols::table::tests::top_level_symbol_lookup_policy_is_first_writer_for_all_insert_apis -- --nocapture` failed before the fix because `add_symbol` pointed global lookup at the second duplicate, then passed after both insertion paths used first-writer global lookup policy.

## Phase 5 - HIR Declaration Catalog And Runtime Parity

- [x] `HIRZSB-P5-001` Add a HIR-owned project declaration catalog with qualified name, owner scope, source file/range, declaration kind, callable/value/type role, and unresolved references. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test declaration_catalog -- --nocapture` first failed because `trust_hir::semantic` was private and `FileAnalysis::declaration_catalog` did not exist, then passed after HIR analysis built `DeclarationCatalog` records from `SymbolTable` with qualified names, source identity, owner symbol/scope, owned scope, declaration kind, semantic role, imported flag, and retained OOP references.
- [x] `HIRZSB-P5-002` Resolve declaration references through the semantic kernel and retain exact unresolved/ambiguous/wrong-kind outcomes. Focused evidence: `declaration_catalog_exposes_qualified_declarations_roles_and_sources` asserts the catalog retains `Probe IMPLEMENTS IProbe` as a resolved reference outcome instead of a raw string; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_interfaces -- --nocapture` passed 13 interface/extends/conformance tests after `EXTENDS`/`IMPLEMENTS` storage moved to semantic reference records.
- [x] `HIRZSB-P5-003` Include project imports and initializer/type identity mappings in the catalog or an adjacent semantic index. Focused evidence: `declaration_catalog_marks_project_imports_and_translated_type_identity` verifies a project-imported `Lib.Probe` catalog entry keeps `source.file_id() == FileId(0)`, `is_imported() == true`, and a translated non-`UNKNOWN` `TypeId` when analyzed from `FileId(1)`; initializer identity remains in the adjacent HIR initializer catalog covered by `HIRZSB-P2-005` and `HIRZSB-P3-007`.
- [x] `HIRZSB-P5-004` Expose a runtime-facing declaration catalog view that does not let runtime own declaration discovery. Focused evidence: `FileAnalysis::declaration_catalog` now exposes the public runtime-facing view, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test initializer_architecture runtime_pou_registration_is_hir_catalog_driven -- --nocapture` verifies runtime POU entry-point registration takes `DeclarationCatalog`.
- [x] `HIRZSB-P5-005` Switch runtime declaration registration to catalog/parity-driven discovery while keeping syntax walks only for body lowering. Focused evidence: runtime `predeclare_function_blocks`, `predeclare_classes`, `predeclare_interfaces`, `lower_programs`, `lower_functions`, `lower_function_blocks`, `lower_classes`, and `lower_interfaces` now iterate catalog entries and use syntax only to find the exact body node by identifier range; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test e2e_multifile -- --nocapture` passed all 4 namespaced/multifile runtime tests.
- [x] `HIRZSB-P5-006` Make catalog/lowering mismatch an error with a focused diagnostic instead of a silent skip. Focused evidence: `catalog_pou_nodes` returns `CompileError` messages beginning `HIR declaration catalog/lowering mismatch` when a catalog entry has no matching body node or matches multiple nodes; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test initializer_architecture runtime_pou_registration_is_hir_catalog_driven -- --nocapture` asserts that explicit mismatch path remains present.
- [x] `HIRZSB-P5-007` Replace `SymbolTable::extends` and `SymbolTable::implements` raw-string storage with catalog references or semantic-kernel reference records; do not merely shadow the raw strings with a second catalog. Focused evidence: `SymbolTable` now stores OOP references as `OopReference` semantic records, exposes `extends_reference`/`implements_references`, and builds declaration-catalog references from those records; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_interfaces -- --nocapture` passed after the storage change.
- [x] `HIRZSB-P5-008` Once the HIR declaration catalog exists, the runtime semantic-bypass doctor rule must fail on bypasses rather than only report a finding. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` now scans runtime POU registration/predeclaration bypass patterns as `HIRZSB-FAIL-RUNTIME-DECL-BYPASS`, and `python3 scripts/hir_zero_silent_bug_doctor.py` exits 0 with no fail-class runtime bypass findings while preserving warn-only HIR migration findings.
- [x] `HIRZSB-P5-009` After catalog migration, remove or narrow `set_extends`, `set_implements`, `extends_name`, and `implements_names` to `pub(crate)` and expose catalog APIs instead of public raw-string mutation/access. Focused evidence: raw mutators/accessors are now `pub(crate)`, external `trust-ide` callers use `extends_reference`, `implements_references`, or scoped OOP reference resolution, and `RUSTUP_TOOLCHAIN=1.95 cargo check -p trust-ide --all-targets` passed.

## Phase 6 - Architecture Doctor And Mutation Gates

- [x] `HIRZSB-P6-001` Add a full-map doctor rule that blocks new HIR broad lookup calls outside the semantic kernel allowlist. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` scans production HIR source for `lookup_any`, `resolve_by_name`, and broad `lookup_type` calls while excluding test fixtures; production broad lookups in shared-global diagnostics, unused-symbol diagnostics, access-path resolution, and `SemanticDatabase::resolve_name` were replaced with scoped/global/qualified or unique-name resolution. Validation: `python3 scripts/hir_zero_silent_bug_doctor.py | rg "HIRZSB-WARN-BROAD-LOOKUP"` returns no matches, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking assignments_and_var_access -- --nocapture` passed 64 tests, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections -- --nocapture` passed 6 tests.
- [x] `HIRZSB-P6-002` Add a full-map doctor rule that blocks duplicated resolver/type-resolution/import-resolution logic outside the semantic kernel. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` now collects resolver function definitions across production HIR source, flags duplicated resolver names outside an explicit wrapper allowlist, and no longer treats every function containing `resolve` as a duplicate. The rule exposed the real duplicate `resolve_program_type` pair; configuration diagnostics and shared-global task diagnostics now share `configuration::resolve_program_type` / `ProgramTypeResolution` instead of carrying separate implementations. Validation: `python3 -m py_compile scripts/hir_zero_silent_bug_doctor.py` passed and `python3 scripts/hir_zero_silent_bug_doctor.py` reports `HIR zero-silent-bug doctor: no findings`.
- [x] `HIRZSB-P6-003` Add a full-map doctor rule that flags diagnostic-bearing semantic functions returning ambiguous `Option` without an explicit suppression/outcome type. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` now emits `HIRZSB-WARN-LOSSY-RESULT-OPTION` for lossy `try_*().ok()` collapses in production HIR semantic source, including the old const-eval bridge shape; `python3 -m py_compile scripts/hir_zero_silent_bug_doctor.py && python3 scripts/hir_zero_silent_bug_doctor.py --self-test && python3 scripts/hir_zero_silent_bug_doctor.py` passes with known-bad self-test coverage and `HIR zero-silent-bug doctor: no findings`.
- [x] `HIRZSB-P6-004` Add a full-map doctor rule or report finding for runtime declaration discovery bypasses after the HIR catalog exists. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` now emits fail-class `HIRZSB-FAIL-RUNTIME-DECL-BYPASS` findings for direct runtime POU registration/predeclaration syntax scans; `python3 scripts/hir_zero_silent_bug_doctor.py | rg "HIRZSB-FAIL-RUNTIME"` returns no matches, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test initializer_architecture runtime_pou_registration_is_hir_catalog_driven -- --nocapture` passed.
- [x] `HIRZSB-P6-005` Add known-bad tests or fixtures for every new doctor rule. Focused evidence: `python3 scripts/hir_zero_silent_bug_doctor.py --self-test` now checks known-bad fixtures for broad lookup, typed sentinel returns, lossy `try_*().ok()` result collapse, silent collision-return discard, `Type::Unknown` fallback, duplicated resolver definitions, allowlist limit/granularity, runtime declaration bypasses, and public raw HIR APIs; the self-test prints `HIR zero-silent-bug doctor self-test: ok`.
- [x] `HIRZSB-P6-006` Run focused mutation shards for the semantic kernel, identity maps, resolver outcomes, and diagnostic suppression paths. Focused evidence: `cargo mutants` ran serial (`--jobs 1`) shards under `target/gate-artifacts/hir-zero-mutants/`: `semantic-outcome-rerun` tested 10 mutants with 2 caught, 8 unviable, 0 missed after `semantic_outcome_is_resolved_only_for_resolved_variant`; `project-identity-rerun3` tested 3 mutants with 2 caught, 1 unviable, 0 missed after the leading `./` normalization display test; `context-resolvers-rerun` tested 6 mutants with 3 caught, 3 unviable, 0 missed after missing-name/function-return/method-return context tests; `is-assignable-assignments-rerun` tested 7 mutants with 7 caught, 0 missed after the numeric widening assignment compatibility test.
- [x] `HIRZSB-P6-007` Reduce unexplained HIR zero-silent-bug mutation survivors to zero or document equivalent mutants with source-level rationale. Focused evidence: the P6-006 reruns above had 0 missed mutants across all rerun shards; no equivalent-mutant waiver was needed because every original survivor was either killed by a focused behavior-lock test or classified unviable by cargo-mutants.
- [x] `HIRZSB-P6-008` Add a doctor rule that blocks new diagnostic-bearing `TypeId::UNKNOWN` / `SymbolId::UNKNOWN` sentinel returns outside the explicit semantic-kernel allowlist. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` scans production HIR source for direct `return TypeId::UNKNOWN`, `return SymbolId::UNKNOWN`, match-arm sentinel returns, and `unwrap_or(TypeId::UNKNOWN)`/`unwrap_or(SymbolId::UNKNOWN)` patterns; after the P3-005 migration, `python3 scripts/hir_zero_silent_bug_doctor.py` reports `HIR zero-silent-bug doctor: no findings`. Validation: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking -- --nocapture` passed 285 tests, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test var_sections -- --nocapture` passed 6 tests, and `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test declaration_catalog -- --nocapture` passed 2 tests.
- [x] `HIRZSB-P6-009` Add a doctor rule that blocks `let _ = ...` discards of collision-bearing semantic returns such as `define_in_scope`, `insert`, and similar map/table updates in HIR semantic crates. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` scans production HIR source for silent `let _ = ...define_in_scope/insert(...)` discards, and `python3 scripts/hir_zero_silent_bug_doctor.py | rg "HIRZSB-WARN-SILENT-DISCARD"` returns no matches after project symbol import collision handling was routed through explicit collision records.
- [x] `HIRZSB-P6-010` Extend mutation shards to `type_check/expr.rs`, `type_check/calls.rs`, `type_check/standard/*`, `symbol_import.rs` `define_in_scope` discards, `symbol_import.rs::import_type` cycle handling, and every `eval_const_int_expr` Option bridge caller. Focused evidence: `scripts/hir_zero_silent_bug_mutation_shards.sh` defines serial `cargo mutants --jobs 1` shards for semantic outcomes, project identity, context resolvers, `is_assignable`, `type_check/expr.rs`, `type_check/calls.rs`, type-check and collector const-eval bridges, symbol import collision/cycle paths, and standard type-check files. Final focused mutation campaign under `target/gate-artifacts/hir-zero-mutants/` covered 529 mutants total with 449 caught, 80 unviable, 0 missed, and 0 timeouts.
- [x] `HIRZSB-P6-011` Decide diagnostic deduplication before semantic-kernel rollout: either deduplicate by code/range/message at builder finish or document why duplicate diagnostics are acceptable and tested. Decision: exact duplicate diagnostics are deduplicated at `DiagnosticBuilder::add` by full diagnostic identity, preserving distinct codes/messages/ranges/related information while preventing repeated probes from emitting the same primary diagnostic multiple times. Focused evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test namespaces using_ambiguous_assignment_target_reports_one_primary -- --nocapture` failed before deduplication with three identical `CannotResolve` diagnostics, then passed; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking sizeof_semantics -- --nocapture` passed the `SIZEOF` ambiguity shard.
- [x] `HIRZSB-P6-012` Add a doctor rule that loads the semantic-kernel allowlist file and fails if it has more than 5 entries or if an entry is broader than one function/file-line range. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` now validates `docs/internal/architecture/hir-semantic-kernel-allowlist.toml` for `max_entries`, entry count, and one-function or one-`file_range` granularity; `python3 scripts/hir_zero_silent_bug_doctor.py | rg "HIRZSB-ALLOWLIST"` returns no matches.
- [x] `HIRZSB-P6-013` Add a doctor rule that blocks `unwrap_or(&Type::Unknown)`, `unwrap_or(Type::Unknown)`, and equivalent inline `Type::Unknown` substitutions in HIR semantic crates outside the kernel allowlist. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` scans production HIR source for inline `Type::Unknown` substitutions, and `python3 scripts/hir_zero_silent_bug_doctor.py | rg "HIRZSB-WARN-TYPE-UNKNOWN-FALLBACK"` returns no matches after compatibility checks stopped substituting missing array/pointer identities with `Type::Unknown`.
- [x] `HIRZSB-P6-014` Add a doctor rule or explicit source check for public HIR APIs that expose raw broad lookups or raw extends/implements mutation after the catalog migration. Focused evidence: `scripts/hir_zero_silent_bug_doctor.py` now emits fail-class `HIRZSB-FAIL-PUBLIC-RAW-API` for public `lookup_any`, `set_extends`, `set_implements`, `extends_name`, and `implements_names`; `python3 scripts/hir_zero_silent_bug_doctor.py | rg "HIRZSB-FAIL-PUBLIC-RAW-API"` returns no matches, and `RUSTUP_TOOLCHAIN=1.95 cargo check -p trust-ide --all-targets` passed after external callers moved to reference APIs.

## Phase 7 - OSCAT Aggregate Decision

- [x] `HIRZSB-P7-001` Keep the default OSCAT catalog gate structural/fast while this board is open. Evidence: `oscat_oop_example_st_unit_tests_pass` remains ignored as the expensive 98-project sweep, while the default `oscat_oop_examples` target runs structural checks plus the focused aggregate trigger.
- [x] `HIRZSB-P7-002` Attempt the one-compile OSCAT aggregate only after `HIRZSB-P1` through `HIRZSB-P6` focused gates pass. Evidence: after focused mutation and doctor gates, `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-runtime --test oscat_oop_examples -- --nocapture` ran the aggregate trigger.
- [x] `HIRZSB-P7-003` If aggregate still fails, classify each failure as syntax, HIR semantic kernel, runtime lowering, project/import identity, or OSCAT example limitation before patching. Evidence: first aggregate failure was classified as HIR wrong-kind handling for function/method return-variable reads and fixed with focused red tests; second failure was classified as CPU-bound aggregate compile exceeding the 60s child timeout, and the timeout was raised to 120s after a manual run completed in 72s with 3 ST tests passing.

## Focused Validation

- [x] `HIRZSB-VAL-01` Run focused HIR tests added by this board. Evidence: `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --lib -- --nocapture` passed 73 tests; `RUSTUP_TOOLCHAIN=1.95 cargo test -p trust-hir --test semantic_type_checking -- --nocapture` passed 289 tests; `namespaces`, `semantic_interfaces`, `declaration_catalog`, `var_sections`, and `semantic_standard_functions` focused targets passed.
- [x] `HIRZSB-VAL-02` Run the existing focused import, const-eval, and aggregate initializer tests affected by the semantic kernel. Evidence: `semantic_type_checking` passed 289 tests including cross-file import, const-eval primary diagnostics, aggregate initializer, wrong-kind, and return-variable regression coverage.
- [x] `HIRZSB-VAL-03` Run `cargo test -p trust-runtime --test e2e_multifile namespaced_ -- --nocapture`. Evidence: passed 2 namespaced runtime tests.
- [x] `HIRZSB-VAL-04` Run `cargo test -p trust-runtime --test oscat_oop_examples -- --nocapture` for the fast structural gate. Evidence: passed 5 tests with 1 ignored expensive sweep; aggregate child completed successfully in 70.591s under the 120s timeout.
- [x] `HIRZSB-VAL-05` Run `cargo run -p xtask -- architecture-doctor --full-map` after doctor rules are added. Evidence: command passed and wrote `target/gate-artifacts/full-software-map-00c171fbb/`; expected tracked partial/finding items remained non-fatal.
- [x] `HIRZSB-VAL-06` Run focused mutation shards before marking this board complete. Evidence: 17 focused `cargo mutants --jobs 1` shards completed with total 529 mutants: 449 caught, 80 unviable, 0 missed, 0 timeouts.
- [x] `HIRZSB-VAL-07` Run `just fmt`, `just clippy`, and `just test-all` only for final merge/release/board-completion readiness under the staged cadence rule. Evidence: `RUSTUP_TOOLCHAIN=1.95 just fmt` passed, `RUSTUP_TOOLCHAIN=1.95 just clippy` passed, and `RUSTUP_TOOLCHAIN=1.95 just test-all` passed. The final full test gate used `scripts/cargo_test_fast_link.sh`, used `mold` for Rust test linking, and no longer ran the `complete_program` integration target twice; the first full run after changing `RUSTFLAGS` rebuilt the test artifacts once before the cache was warm.
- [x] `HIRZSB-VAL-08` Record the semantic-kernel allowlist and typed-sentinel allowlist as artifacts before merge. Evidence: `docs/internal/architecture/hir-zero-silent-bug-allowlist-artifacts-2026-04-30.md` records the 5-entry semantic-kernel compatibility allowlist, no active broad typed-sentinel allowlist, no active silent-discard allowlist, and doctor validation.
- [x] `HIRZSB-VAL-09` Verify the semantic kernel has no static/thread-local mutable state and remains safe under Salsa incremental recomputation with an LSP-style edit-and-recheck test. Evidence: `rg -n "static |static mut|thread_local!|lazy_static|OnceLock|LazyLock|Mutex<|RwLock<" crates/trust-hir/src/semantic.rs crates/trust-hir/src/db crates/trust-hir/src/type_check crates/trust-hir/src/symbols crates/trust-hir/src/project.rs` found only legitimate DB-owned mutex/state and static string return types, not semantic-kernel global mutable state; `semantic_kernel_cross_file_resolution_tracks_lsp_style_dependency_edits` passed inside `cargo test -p trust-hir --lib`.

## Exit Criteria

- [x] `HIRZSB-EXIT-01` All diagnostic-bearing HIR semantic decisions use the semantic kernel or an explicit allowlisted compatibility path. Evidence: semantic-kernel allowlist artifact has exactly 5 one-function entries and doctor scan reports no findings.
- [x] `HIRZSB-EXIT-02` Resolver and validation outcomes preserve unknown/ambiguous/wrong-kind/suppressed-cascade distinctions until diagnostics or explicit cascade suppression. Evidence: `semantic_type_checking` passed wrong-kind, ambiguity, suppression-primary, const-eval, return-variable, and import-cycle tests.
- [x] `HIRZSB-EXIT-03` HIR semantic keys use full project/scope/source identity where language semantics require it. Evidence: `namespaces`, `declaration_catalog`, `var_sections`, project identity lib tests, and project mutation shards passed with 0 missed mutants.
- [x] `HIRZSB-EXIT-04` Runtime declaration discovery is HIR catalog/parity driven and cannot silently miss declarations HIR accepted. Evidence: runtime namespaced multifile gate passed and full-map doctor reported no runtime declaration bypass failures.
- [x] `HIRZSB-EXIT-05` Full-map doctor blocks new HIR broad lookup, duplicated resolver, ambiguous suppression, and runtime semantic-bypass paths. Evidence: HIR zero-silent-bug doctor reported no findings, and `cargo run -p xtask -- architecture-doctor --full-map` passed.
- [x] `HIRZSB-EXIT-06` Focused mutation shards for the semantic kernel and identity maps have zero unexplained survivors. Evidence: final mutation campaign had 0 missed and 0 timeout mutants; no survivor waivers needed.
- [x] `HIRZSB-EXIT-07` OSCAT aggregate replacement is either safely enabled behind passing parity tests or explicitly deferred with classified blockers. Evidence: default fast structural gate plus the focused one-compile OSCAT aggregate trigger passed; the expensive 98-project sweep remains explicitly ignored.
- [x] `HIRZSB-EXIT-08` Semantic-kernel compatibility allowlist has at most 5 entries, and typed-sentinel/discard allowlists are explicit, tested, and owner-reviewed. Evidence: `hir-zero-silent-bug-allowlist-artifacts-2026-04-30.md` records exactly 5 compatibility entries and no active broad typed-sentinel or discard allowlists.
