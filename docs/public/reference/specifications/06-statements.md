# Statements

This public entry covers assignments, calls, selection/iteration statements,
`RETURN`, `EXIT`, `CONTINUE`, labels, and `JMP`.

Use the full spec below when you need exact statement syntax, label resolution,
or the current reachability-analysis boundary.

Related: [Expressions](05-expressions.md), [Runtime](10-runtime-semantics.md)

--8<-- "docs/specs/06-statements.md"
