# Runtime Semantics

This public entry covers runtime values, execution, memory, I/O semantics, and
the standard-library execution model.

Use the full spec below when you need the normative runtime behavior rather
than deployment, bytecode, or debug-adapter details.

Related: [Statements](06-statements.md), [Concepts -> Runtime Model](../../concepts/runtime-model.md)

--8<-- "docs/specs/10-runtime-semantics.md"
