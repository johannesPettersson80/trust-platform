# Airport Baggage Command Observer OOP Project

This is one runnable project inside the paired OSCAT example.

Read the teaching README one level up:

`../README.md`

Run this project with:

```bash
trust-runtime test --project examples/OSCAT/airport_baggage_command_observer/oop
```
